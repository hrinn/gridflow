<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.Cursor?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.Pane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.AnchorPane?>
<?import java.lang.Double?>
<?import javafx.scene.control.Tooltip?>
<?import javafx.scene.control.ToggleGroup?>


<?import javafx.scene.layout.HBox?>
<AnchorPane
        fx:controller="construction.BuildMenuViewController"
        xmlns="http://javafx.com/javafx/11.0.2"
        xmlns:fx="http://javafx.com/fxml/1"
        maxHeight="-Infinity" maxWidth="Infinity" minHeight="-Infinity" minWidth="-Infinity" >
    <fx:define>
        <Double fx:id="ButtonSize" fx:value="48.0"/>
    </fx:define>
        <VBox alignment="TOP_CENTER" AnchorPane.bottomAnchor="0.0" AnchorPane.topAnchor="0.0" style="-fx-background-color: LIGHTGRAY">
            <Button fx:id="InteractToolButton" onMousePressed="#selectInteractTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/InteractIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Interact"/></tooltip>
            </Button>
            <Button fx:id="SelectToolButton" onMousePressed="#selectSelectTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/SelectIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Select"/></tooltip>
            </Button>
            <Button fx:id="AssociationToolButton" onMousePressed="#selectAssociationTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/AssociateIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Association"/></tooltip>
            </Button>
            <Button fx:id="WireButton" onMousePressed="#selectWireTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/WireIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Wire"/></tooltip>
            </Button>
            <Button fx:id="PowerSourceButton" onMousePressed="#selectPowerSourceTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/PowerSourceIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Power Source"/></tooltip>
            </Button>
            <Button fx:id="TurbineButton" onMousePressed="#selectTurbineTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/TurbineIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Turbine"/></tooltip>
            </Button>
            <Button fx:id="SwitchButton" onMousePressed="#selectSwitchTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/SwitchIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Switch"/></tooltip>
            </Button>
            <Button fx:id="Breaker12Button" onMousePressed="#selectBreaker12Tool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/Breaker12KVIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="12KV Breaker"/></tooltip>
            </Button>
            <Button fx:id="Breaker70Button" onMousePressed="#selectBreaker70Tool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/Breaker70KVIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="70KV Breaker"/></tooltip>
            </Button>
            <Button fx:id="TransformerButton" onMousePressed="#selectTransformerTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/TransformerIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Transformer"/></tooltip>
            </Button>
            <Button fx:id="JumperButton" onMousePressed="#selectJumperTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/JumperIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Jumper"/></tooltip>
            </Button>
            <Button fx:id="CutoutButton" onMousePressed="#selectCutoutTool" prefHeight="$ButtonSize" prefWidth="$ButtonSize" stylesheets="@style.css">
                <graphic>
                    <ImageView fitHeight="$ButtonSize" fitWidth="$ButtonSize" pickOnBounds="true" preserveRatio="true">
                        <image>
                            <Image url="@../resources/CutoutIcon.png" />
                        </image>
                    </ImageView>
                </graphic>
                <tooltip><Tooltip text="Cutout"/></tooltip>
            </Button>
        </VBox>
</AnchorPane>